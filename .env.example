# Browser-safe feature flag. Never place an OpenAI API key in a VITE_* variable.
VITE_ATLAS_CHATGPT_ENABLED=false

# Server-only settings. Integration stays disabled until explicitly changed.
ATLAS_OPENAI_ENABLED=false
ATLAS_API_PORT=8787
OPENAI_API_KEY=
OPENAI_MODEL=
